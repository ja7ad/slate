[package]
name = "slate-sim"
version.workspace = true
edition.workspace = true

[dependencies]
slate-hal = { path = "../slate-hal" }
rand = { version = "0.8", features = ["small_rng"] }
proptest = "1.5"
slate-core = { path = "../slate-core" }
