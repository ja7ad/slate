[package]
name = "slate-core"
version.workspace = true
edition.workspace = true

[dependencies]
slate-hal = { path = "../slate-hal" }
slate-erasure = { path = "../slate-erasure" }
sha2 = { version = "0.11.0", default-features = false }
