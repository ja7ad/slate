[package]
name = "slate-esp32"
version = "0.1.0"
edition = "2021"

[dependencies]
slate-core = { path = "../../crates/slate-core" }
slate-hal = { path = "../../crates/slate-hal" }
slate-crypto = { path = "../../crates/slate-crypto" }

esp-hal = { version = "1.1.1", default-features = false }
esp-backtrace = { version = "0.19.0", features = ["panic-handler", "println"] }
esp-println = { version = "0.17.0" }
esp-storage = { version = "0.9.0" }
embedded-storage = "0.3.1"

[features]
default = []
chip-esp32c3 = ["esp-hal/esp32c3", "esp-hal/rt", "esp-backtrace/esp32c3", "esp-println/esp32c3", "esp-storage/esp32c3"]
counter-flash = []
counter-efuse = []
counter-none = []
